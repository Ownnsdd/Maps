<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#FFFFFF"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/detail_name"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:paddingLeft="20dp"
        android:gravity="center"
        android:textSize="20sp"
        android:textColor="#000000"
        android:text="@string/detail_name" />

    <EditText
        android:id="@+id/detail_name_edit"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:gravity="center_vertical"
        android:hint="name"
        android:textSize="15dp"
        android:layout_toRightOf="@+id/detail_name"
        android:layout_toEndOf="@+id/detail_name" />

    <TextView
        android:id="@+id/detail_phone"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:paddingLeft="20dp"
        android:gravity="center"
        android:textSize="20sp"
        android:textColor="#000000"
        android:layout_below="@+id/detail_name" 
        android:text="@string/detail_phone"/>

    <EditText
        android:id="@+id/detail_phone_edit"
        android:inputType="phone"
        android:layout_width="150dp"
        android:layout_height="50dp"
        android:gravity="center_vertical"
        android:textSize="15dp"
        android:hint="phone"
        android:layout_toRightOf="@+id/detail_phone"
        android:layout_below="@+id/detail_name_edit"
        android:layout_toEndOf="@+id/detail_phone" />

    <Button
        android:id="@+id/detail_save_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:text="Save" />

</RelativeLayout>